import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#186048",
			lighter: "#189078",
			lighter_2: "#30a878",
			lighter_3: "#90d890",
			light: "#a8f0a8",
			highlight: "#d8f0c0",
		},
	},
});

export default theme;
